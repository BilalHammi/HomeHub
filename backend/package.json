{
  "name": "backend",
  "version": "1.0.0",
  "description": "",
  "license": "ISC",
  "author": "",
  "main": "index.js",
  "scripts": {
    "start": "npm run build:live",
    "build": "npx tsc --watch",
    "debug": "nodemon --watch 'src/**/*.ts' --exec \"ts-node\" src/index.ts",
    "test": "mocha --require ts-node/register --extensions ts,tsx --watch --watch-files src,test 'src/test/**/*.{ts,tsx}'",
    "build:live": "nodemon --watch 'src/**/*.ts' --exec \"ts-node\" src/index.ts --file test/mocha-setup.ts"
  },
  "dependencies": {
    "express": "^5.1.0"
  }
}
